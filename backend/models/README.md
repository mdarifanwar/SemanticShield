# Models directory
# Place trained model files here:
#   plagiarism_model.pkl  — trained RandomForestClassifier
#   metrics.json          — training/evaluation metrics
#
# Generate with:
#   cd backend && python ../scripts/train.py
